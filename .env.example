# Plantilla de configuración de la base de datos.
# Copie este archivo como `.env` (mismo directorio) y reemplace los valores.
# `.env` está excluido por .gitignore: NUNCA suba credenciales reales al repositorio.
DB_HOST=localhost
DB_PORT=3306
DB_NAME=techstore_db_df
DB_USER=root
DB_PASSWORD=
